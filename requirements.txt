# Pile native FIGÉE pour Streamlit Cloud (Python 3.13).
# Sans ces bornes, uv résout pandas/numpy/pyarrow vers du bleeding-edge
# (pandas 3.0 + pyarrow 25) qui provoque un SEGMENTATION FAULT côté serveur :
#  - pyarrow 25 est buggé au point que Streamlit 1.59 l'a plafonné (pyarrow<25) ;
#  - pandas 3.0 stocke par défaut les chaînes via pyarrow → toutes les colonnes
#    texte des grilles passent par cette couche instable.
# Ces versions ont des wheels cp313 et respectent les bornes de streamlit 1.58
# (numpy<3, pandas<4, pyarrow>=7).
pandas==2.2.3
numpy==2.1.3
pyarrow==18.1.0

streamlit==1.58.0
requests>=2.31
streamlit-aggrid==1.2.1.post2
anthropic>=0.68
playwright>=1.40
pyvirtualdisplay>=3.0
